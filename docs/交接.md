# 交接

- 更新时间：2026-08-03
- 工作目录：`D:\RemoteController`

## 当前目标

诊断并修复 `Setup-RemoteControllerAgent.cmd` 报“exit code 1”的失败，确认修复不会把已完成的服务安装误判为失败，并保留真实目标机验证边界。

## 已完成

+ 确认 `.cmd` 的退出码只是 PowerShell 子脚本的 `$LASTEXITCODE`，不能单独说明失败阶段。
+ 确认根因是 `scripts/Setup-RemoteControllerAgent.ps1:232` 在 `StrictMode` 下读取 Agent identity JSON 中不存在的 `paired` 属性。
+ 将该字段改为安全属性检查；缺失时输出未报告，而不是抛出 `PropertyNotFoundStrict`。
+ 重新生成 `artifacts/publish`，并确认源脚本与发布脚本 SHA-256 一致。
+ 完成 PowerShell AST、Release build 和单元测试验证：0 warning/0 error，252/252 通过。
+ 已按全局协议创建并更新 `docs/交接.md`、`docs/审计.md`；旧进度记录保留，并在 `docs/CURRENT_PROGRESS.md` 追加本次诊断证据。

## Next Steps（优先级）

1. 将新的 `artifacts\publish` 完整包复制到目标机，并在目标机运行修复后的 `Setup-RemoteControllerAgent.cmd`。
2. 若目标机已有配对，先把配置中的 `RegenerateIdentity` 设为 `false`；仅在明确需要换证书并准备重新配对时保留 `true`。
3. 采集真实目标机的完整 PowerShell 输出、服务状态、Agent identity/fingerprint，并逐项验证 Controller 的 probe/pair/exec；不要把本地构建或单元测试当作端到端通过。

## 活跃上下文文件

- `scripts/Setup-RemoteControllerAgent.ps1`
- `scripts/Setup-RemoteControllerAgent.cmd`
- `src/Rc.Agent/Security/LocalAdminCommand.cs`
- `docs/CURRENT_PROGRESS.md`
- `docs/审计.md`
- `artifacts/publish/`（本次重新生成的未跟踪发布目录）

## 特殊指令

- 保留用户现有未提交修改；不要重置或覆盖无关的 CLI、README、发布脚本和测试改动。
- 真实目标机初始化会停止服务并可能轮换 TLS 身份；未获得明确验证证据前，不声称部署或配对已通过。
- 控制端私钥、Agent 私钥、一次性配对码和 TLS 指纹应避免写入普通日志或聊天。

## 2026-08-03 收尾复核

+ `git diff --check` 退出码为 0；工作树中的其他未提交改动已保留，未执行重置、清理或覆盖。

## 2026-08-03 提交前验证

+ 重新执行 `dotnet test Rc.RemoteController.sln --configuration Release --no-build --verbosity minimal`，四个测试程序集 252/252 通过。
+ 已按当前工作树内容选择性暂存相关源码、脚本、测试和文档；未包含 `artifacts/publish` 等忽略目录。

## 2026-08-03 最新代码同步

+ 使用 `git pull --ff-only origin main` 完成快进同步：`7d6f5cf` → `91223e5`。
+ 最新提交为 `fix: reject duplicate target fingerprints`，涉及目标参数解析及对应测试。
+ 同步前后未发现本地未提交改动；本次未运行测试、未修改代码。

## 2026-08-03 配对码未显示诊断

+ 当前仓库版本在 `scripts/Setup-RemoteControllerAgent.ps1:211-218` 明确调用 `arm-pairing` 并输出配对码；`scripts/RemoteController.Agent.config.json:9` 默认 `ArmPairing=true`。
+ 若成功输出中出现“Pairing code generation is disabled by configuration.”，实际使用的配置将 `ArmPairing` 设为了 `false`。
+ 若目标机配置为 `true` 但没有 `[5/5]` 配对阶段或配对码，优先检查是否运行了旧发布包，或是否只查看了启动 `.cmd` 的父窗口；`.cmd:5-6` 会在新窗口中执行真实安装流程。
+ 本次只完成源码/配置/启动链路诊断，未在真实目标机重跑初始化，未修改代码。

## 2026-08-07 提交前复核

+ 当前未提交范围：`scripts/Setup-RemoteControllerAgent.cmd`、`scripts/Setup-RemoteControllerAgent.ps1`、`docs/交接.md`、`docs/审计.md`。
+ PowerShell AST：Pass；`scripts/Setup-RemoteControllerAgent.ps1` 语法错误数为 0。
+ `dotnet test Rc.RemoteController.sln --configuration Release --no-restore --verbosity minimal`：Pass，四个测试程序集共 253/253（Contracts 118、TaskHost 15、PrivilegedBroker 3、Agent 117）。
+ `git diff --check`：Pass；仅有 Git 的 LF/CRLF 转换提示。
+ 本次仍未执行真实目标机初始化、服务安装、配对或远程链路验证。

## 2026-08-07 远程提交准备

+ 用户确认将当前 4 个修改文件提交并推送到 `origin/main`。
+ 提交前保留真实目标机验证边界；不将本地测试结果表述为端到端通过。

## 2026-08-07 目标机安装失败初步诊断

+ 用户提供的实际输出在 `[1/5] Installing or refreshing the service package...` 后直接进入通用 exit code 1，未包含 PowerShell 异常正文。
+ 当前源码路径：`Setup-RemoteControllerAgent.ps1:194-196` 调用 `Install-RemoteController.ps1`；该阶段可能失败于停止旧服务/UI 进程、复制发布文件、`sc.exe` 服务配置或后续安装权限操作。
+ 已核对 `scripts/` 与 `artifacts/publish/` 中安装脚本 SHA-256 一致；当前发布包不是缺少 `Install-RemoteController.ps1`。
+ 只执行了 `Install-RemoteController.ps1 -WhatIf` 模拟检查，退出码 0；未执行真实安装、未修改服务/文件/配置。
+ 已确认本机存在 `RemoteControllerAgent` 与 `RemoteControllerBroker` 服务且当前为 Stopped；这不能替代失败时的目标机异常证据。

## Next Steps（优先级）

1. 在目标机重新运行并保留完整 PowerShell 异常正文，尤其是 `[1/5]` 后第一条 `Exception`、`CategoryInfo` 和 `FullyQualifiedErrorId`。
2. 若异常指向服务配置，采集 `sc.exe qc RemoteControllerAgent`、`sc.exe qc RemoteControllerBroker` 及安装进程完整输出后再修复。
3. 获得根因后再修改脚本并重新生成/校验 `artifacts/publish`；不要用 `-WhatIf` 或本地测试替代真实目标机验证。

## 2026-08-07 日志采集结果

+ 用户按建议执行管理员 PowerShell 管道命令，但反馈内容只包含最终的日志路径，没有 `[1/5]` 或异常正文。
+ 当前 Codex 会话无法读取该路径下的 `C:\Users\YUKIKAZE\AppData\Local\Temp\RemoteController-Setup-20260807-183408.log`，文件在本会话环境中不存在；不能据此推断安装成功或失败根因。
+ 仍需用户在执行安装的同一 PowerShell 会话中确认 `Test-Path` 和 `Get-Content` 的实际结果，并提供完整日志。

## 2026-08-07 根因确认与修复准备

+ 目标机完整异常确认：`artifacts/publish/Setup-RemoteControllerAgent.ps1:176` 报 `Missing package artifact: Rc.Agent.exe`。
+ 根因：从复制发布包运行 Setup 脚本时，配置 `SourcePath="."` 先按配置文件目录解析到 `scripts`；原回退路径固定从 `$PSScriptRoot\..\artifacts\publish` 推导，无法正确覆盖“脚本自身就在 artifacts\\publish”这一入口。
+ 已将回退候选改为优先检查当前脚本目录，再检查仓库 `..\artifacts\publish`；同时改进缺失文件错误信息并让安装脚本从选中的包目录加载。
+ 已同步本地 `artifacts/publish/Setup-RemoteControllerAgent.ps1`，并验证候选选择结果为 `D:\WinAgentController\artifacts\publish`。
+ PowerShell AST：Pass；Release 单元测试 253/253 通过。
+ 尚未在真实目标机重新执行修复后的发布包；尚未提交或推送本次新修复。

## Next Steps（优先级）

1. 使用更新后的完整 `artifacts\\publish` 包覆盖目标机发布目录，再重新运行 Setup。
2. 先在已有配对目标机将 `RegenerateIdentity=false`，避免无必要地轮换 TLS 身份；需要重新配对时再设为 `true`。
3. 保留真实目标机完整输出，逐项验证服务、identity、fingerprint、pair/probe/exec；通过前不宣称端到端完成。

## 2026-08-07 本机 UI 测试交接记录

+ 检查本机已安装服务：`RemoteControllerAgent`、`RemoteControllerBroker` 均为 `Running`；生产端口 `127.0.0.1:43001` 可监听，`Rc.UiTestApp.exe` 窗口标题为 `RemoteController UI Acceptance Test` 且可见。
+ 使用当前发布包的 `Rc.Cli.exe` 对 `127.0.0.1:43001` 完成 TLS probe；服务身份与当前发布包公告指纹一致，且目标报告 `paired: True`。
+ 直接对 `127.0.0.1:43001` 执行 UI 测试时失败：`The session controller identity does not match the paired controller.` 这表明已安装 Agent 的配对控制端不是当前用户的 CLI 控制端，不能把该路径作为当前用户的 UI 端到端通过。
+ 为避免清除或重配现有服务身份，创建了仅用于本机模拟的临时 Agent（用户数据目录、临时 TLS 身份、TCP `43002`）及同用户临时 UI Agent，并使用可见的 `Rc.UiTestApp.exe` 执行测试；临时 Agent/UI Agent 已停止，临时数据已删除，生产服务未停止，测试窗口保留可见。
+ 本机 UI acceptance 重跑结果：Pass；25/25 项均输出 `[PASS]`，脚本最终输出 `UI acceptance passed (25 checks)` 且退出码 0。实际核对包括 reset/invoke、文本输入、下拉选择、树展开折叠、左中右键拖动、滚轮、键盘 A、剪贴板写入/界面读取/读回比对。
+ 第一次临时 UI 运行曾因用户剪贴板为空触发测试脚本恢复阶段异常；第二次运行前写入临时非空值，测试结束后清空剪贴板，避免该测试夹具边界干扰验收结论。

## Next Steps（优先级）

1. 若要验证已安装 `43001` 服务的 UI 链路，需在明确授权后处理现有配对关系，并用当前用户 CLI 重新配对；本次未执行破坏性 unpair/重配。
2. 修复 `scripts/Test-RemoteControllerUi.ps1:241-243` 对空剪贴板的恢复逻辑，再增加空剪贴板回归用例。
3. 将本机 UI acceptance 的 25/25 实际结果与生产服务配对状态分别记录，避免把临时模拟 Agent 结果误报为已安装服务的 UI 端到端结果。

## 当前状态快照（2026-08-07 本机重编译、重装与 UI 验收）

- 更新时间：2026-08-07 20:28:01 +08:00
- 工作目录：D:\WinAgentController

### 当前目标

以最新发布包在本机完成被控端与主控端的一键卸载重装，并将重新生成的生产服务与当前控制端配对后，对可见 UI 测试程序执行实际端到端验收。

### 已完成

+ 通过 scripts\Publish-RemoteController.ps1 -Configuration Release 重新生成 artifacts\publish 完整 Windows x64 自包含包；包中包含 Agent、PrivilegedBroker、TaskHost、UiAgent、UiTestApp、InteractiveTestApp 和 Rc.Cli。
+ 逐项校验完整包文件存在，且发布包内 Setup-RemoteControllerAgent.ps1 与源码 SHA-256 一致。
+ 重新执行 dotnet test Rc.RemoteController.sln --configuration Release --no-restore --verbosity minimal：Pass，253/253（Contracts 118、TaskHost 15、PrivilegedBroker 3、Agent 117）。
+ 使用发布包 Uninstall-RemoteController.cmd 一键卸载：退出码 0；随后确认服务、计划任务、安装/数据目录、43001 监听和产品进程均不存在。
+ 使用发布包 Setup-RemoteControllerAgent.cmd 一键重装：退出码 0；配置的 RegenerateIdentity=true 已按预期清除旧配对并生成新身份，两个服务均启动。
+ 比较 7 个已安装二进制与新发布包的 SHA-256：全部一致；RemoteControllerAgent、RemoteControllerBroker 均为 Running，43001 正在监听，Rc.UiAgent 进程位于当前用户会话。
+ 当前 CLI 控制端已与重装后的本机 Agent 完成一次性配对；TLS 指纹固定 probe 成功，Agent 报告当前控制端已配对。
+ 使用 artifacts\publish\Rc.UiTestApp.exe 的可见窗口在已安装生产服务 127.0.0.1:43001 上执行 Test-RemoteControllerUi.ps1：25/25 项实际界面验证通过，最终输出 UI acceptance passed (25 checks)；测试后剪贴板已恢复。

### Next Steps（优先级）

1. 修复 scripts\Test-RemoteControllerUi.ps1:241-243 的空剪贴板恢复缺陷，并以“初始剪贴板为空”的真实场景重跑 UI 验收；当前 25/25 通过使用了外层测试夹具预填与恢复，不能替代该清理分支的回归。
2. 如需验证跨机器 LAN 链路，在独立控制端重新获取目标指纹并配对，再分别记录网络 TLS probe、UI 会话身份与实际 UI 25 项结果；本次只证明同机 loopback 的已安装生产服务。
3. 若用户要求发布代码，先复核当前 scripts/Setup-RemoteControllerAgent.ps1、docs/交接.md、docs/审计.md 的差异和测试结果，再有意提交/推送；不提交 artifacts/publish。

### 活跃上下文文件

- scripts/Setup-RemoteControllerAgent.ps1
- scripts/Test-RemoteControllerUi.ps1
- scripts/Publish-RemoteController.ps1
- scripts/RemoteController.Agent.config.json
- docs/交接.md
- docs/审计.md
- artifacts/publish/（重新生成的完整发布包；Git 忽略）

### 特殊指令

- RegenerateIdentity=true 会轮换 Agent TLS 身份并清除旧控制端配对；下一次刷新前必须确认这是预期副作用。
- 不在文档、普通日志或聊天中记录 TLS 指纹、一次性配对码或私钥；安装输出可能包含一次性配对码，应按敏感运行日志处理。
- 模拟、单元、构建、同机已安装服务 UI 验收和跨机器 LAN 验收必须继续分开记录。
- Rc.UiTestApp.exe 目前仍保持运行且窗口标题为 RemoteController UI Acceptance Test，便于用户查看；未自动关闭。

## 2026-08-07 初始空剪贴板修复后的本机 UI 重跑

### 当前目标

- 在已安装的 localhost Agent（`127.0.0.1:43001`）上，用可见 `Rc.UiTestApp.exe` 验证初始空剪贴板恢复分支，并保留每项可见状态的实际结果。

### 已完成

+ 源码已改用 `clipboard clear` 显式发送零长度请求；`Test-RemoteControllerUi.ps1` 的 `finally` 在原始剪贴板为空时调用该命令，避免空字符串参数绑定失败。
+ Release 单元测试仍为 Pass：253/253；发布包已重新生成。
+ 本轮于 `2026-08-07 21:20` 启动可见 `D:\WinAgentController\artifacts\publish\Rc.UiTestApp.exe`，运行发布包内 UI 验收脚本。实际通过前 11 项（ready、invoke/value、下拉框、树展开/折叠）。
+ 本轮 UI 验收为 Failed：下一项左键按下期望 `Verification: mouse:down:left`，实际仍为 `Verification: tree:top:collapsed`。原始日志：`D:\WinAgentController\artifacts\test-results\ui-rerun-empty-clipboard-20260807-212046.log`。
+ 本轮失败后对剪贴板的再次读取也返回 `The active UiAgent cannot be reached`；因此未取得本轮 finally 后立即和 1 秒后的空剪贴板实际字节证据。
+ 当前存在服务安装目录 UiAgent 与多个手动启动的 `artifacts\publish\Rc.UiAgent.exe` 并存；其中未按服务 SID 启动的手动实例会注册为活跃实例却无法被 LocalService Agent 连接，导致本轮结果不具备稳定性。

### Next Steps（优先级）

1. 清理本次手动启动的发布目录 UiAgent 实例，或由管理员以最新发布包重新安装后确保只保留一个可连接的 UiAgent，再重跑。
2. 在稳定的单 UiAgent 环境中将剪贴板置为空，运行完整 UI 验收；必须获得 25/25、退出码 0，以及 finally 后立即/1 秒均为 0 字节。
3. 单独诊断鼠标按下事件未更新的原因；失败前已完成的树控件验证不能替代鼠标输入验证。

### 活跃上下文文件

- `src/Rc.UiAgent/DesktopClipboardController.cs`
- `src/Rc.Cli/Commands/UiCommand.cs`
- `scripts/Test-RemoteControllerUi.ps1`
- `artifacts/test-results/ui-rerun-empty-clipboard-20260807-212046.log`

### 特殊指令

- 模拟、单元、构建与真实 UI 结果分开记录；本轮是同机 loopback，且因多 UiAgent 竞争未达到完整真实 UI 通过条件。

## 2026-08-07 单 UiAgent 隔离重跑（21:37）

### 当前目标

- 在排除多 UiAgent 注册竞争后，验证初始剪贴板为空的真实 UI 清理路径，并继续取得完整 25/25 可见 UI 验收证据。

### 已完成

+ 停止一个安装目录 UiAgent 和五个此前手动启动的发布目录 UiAgent，确认测试开始前 `Rc.UiAgent.exe` 数量为 0。
+ 仅启动一个最新 `D:\WinAgentController\artifacts\publish\Rc.UiAgent.exe`，并为 LocalService Agent 设置控制管道 SID 权限；测试期间不存在 UiAgent 注册竞争。
+ 将真实 UI 协议读到的初始剪贴板设置为 0 字节后，启动可见 `Rc.UiTestApp.exe` 并执行完整脚本。
+ 实际通过 20 项：ready、invoke/value、下拉框、树控件，以及左/中/右键的 down、drag、up 均符合预期。
+ 完整 UI 验收仍为 Failed：滚轮操作返回 `Move the mouse within the explicit target before sending a mouse button or wheel event.`，尚未执行后续滚轮、键盘和剪贴板界面读回检查。
+ 初始空剪贴板恢复分支实际通过：即使脚本在滚轮项失败，finally 后立即读取为 0 字节，1 秒后再次读取仍为 0 字节。
+ 测试结束后已停止隔离使用的发布目录 UiAgent，并恢复为单个 `C:\Program Files\RemoteController\Rc.UiAgent.exe`；当前可见测试窗口保留。
+ 日志：`D:\WinAgentController\artifacts\test-results\ui-rerun-single-agent-empty-clipboard-20260807-213741.log`。

### Next Steps（优先级）

1. 修正滚轮测试前的显式目标鼠标定位：在 wheel 操作前重新执行 window-target mouse move，并保留实际 UI 验证。
2. 重新生成发布包后，在单 UiAgent 环境重跑，要求完整 25/25、退出码 0、初始空剪贴板 finally 后立即/1 秒均为 0 字节。
3. 完成后检查最终差异并有意提交；当前不要把“空剪贴板恢复通过”表述成“完整 UI 验收通过”。

### 活跃上下文文件

- `scripts/Test-RemoteControllerUi.ps1`
- `src/Rc.UiAgent/DesktopInputController.cs`
- `src/Rc.UiAgent/DesktopClipboardController.cs`
- `artifacts/test-results/ui-rerun-single-agent-empty-clipboard-20260807-213741.log`

### 特殊指令

- 本轮证明的是同机真实 UI 链路中的空剪贴板 finally 恢复；完整 UI 验收仍因滚轮前置定位要求失败。

## 2026-08-07 滚轮定位修复与完整 UI 通过（21:45）

### 当前目标

- 已完成：修复滚轮操作缺少显式目标定位的问题，并在初始剪贴板为空的单 UiAgent 环境重新执行完整可见 UI 验收。

### 已完成

+ 修改 `scripts/Test-RemoteControllerUi.ps1`：每次 window-target wheel 前重新移动鼠标到测试表面，避免依赖上一条鼠标操作遗留的全局光标位置。
+ PowerShell AST：Pass（0 errors）；`git diff --check`：Pass。
+ 重新执行 `scripts/Publish-RemoteController.ps1 -Configuration Release`：Pass；源码和发布包中的 `Test-RemoteControllerUi.ps1` SHA-256 一致。
+ Release 单元测试：Pass，253/253（Contracts 118、TaskHost 15、PrivilegedBroker 3、Agent 117）。
+ 在仅运行一个最新发布目录 UiAgent、初始剪贴板为 0 字节、可见 `Rc.UiTestApp.exe` 的实际 localhost UI 链路中重跑：25/25 项逐项通过，脚本输出 `UI acceptance passed (25 checks)`，退出码 0。
+ 滚轮修复实际结果：`mouse:wheel:up:4` 与 `mouse:wheel:down:5` 均通过；后续键盘、剪贴板界面读取和协议回读也通过。
+ 空剪贴板 finally 恢复实际结果：脚本结束后立即读取为 0 字节，1 秒后再次读取仍为 0 字节。
+ 测试后已停止隔离使用的发布目录 UiAgent，并恢复为单个安装目录 UiAgent；可见 UI 测试窗口保留。
+ 日志：`D:\WinAgentController\artifacts\test-results\ui-rerun-wheel-fix-empty-clipboard-20260807-214502.log`。

### Next Steps（优先级）

1. 审查当前所有未提交差异，确认空剪贴板、滚轮测试和 Setup 路径修复属于同一提交范围。
2. 如需验证正式安装状态，使用管理员一键刷新最新 `artifacts\publish`（保留身份时设置 `RegenerateIdentity=false`），再做 installed-only 验收。
3. 用户确认后再提交并推送；不要提交 Git 忽略的 `artifacts\publish` 和测试日志。

### 活跃上下文文件

- `scripts/Test-RemoteControllerUi.ps1`
- `src/Rc.UiAgent/DesktopClipboardController.cs`
- `src/Rc.Cli/Commands/UiCommand.cs`
- `artifacts/test-results/ui-rerun-wheel-fix-empty-clipboard-20260807-214502.log`

### 特殊指令

- 本轮 25/25 是“已安装 localhost Agent 服务 + 隔离启动的最新发布目录 UiAgent + 当前 CLI + 可见测试窗口”链路，不等同于最新 UiAgent 已正式覆盖安装目录，也不是跨机器 LAN 验收。

## 2026-08-07 输入操作分离目标确认与重跑（21:50）

### 当前目标

- 将“鼠标定位、鼠标按键、滚轮操作相互分离”明确为项目基本目标：定位只由独立 move 完成，button/wheel 不隐式定位；测试也必须先单独定位，再分别执行和验证按键、滚轮。

### 已完成

+ `README.md` 已在设计原则和命令表中记录输入操作分离目标。
+ `docs/CURRENT_PROGRESS.md` 已在桌面 UI 能力和验收证据中记录该基本目标及测试要求。
+ 调整 `scripts/Test-RemoteControllerUi.ps1`：按键测试继续采用“单独 move → 单独 button”；滚轮序列改为“开始前单独 move 一次 → 单独 wheel up → 单独 wheel down”，不再在每个滚轮步骤中混入定位。
+ PowerShell AST：Pass；Release 发布包重新生成成功；源码与发布包测试脚本 SHA-256 一致；`git diff --check`：Pass。
+ 单 UiAgent、初始剪贴板 0 字节、可见测试窗口的 localhost 实际验收：25/25 通过，退出码 0；滚轮上/下分别通过，finally 后立即和 1 秒剪贴板读回均为 0 字节。
+ 日志：`D:\WinAgentController\artifacts\test-results\ui-rerun-separated-mouse-wheel-empty-clipboard-20260807-215055.log`。
+ 测试后恢复为单个安装目录 UiAgent，可见测试窗口保留。

### Next Steps（优先级）

1. 审查输入操作分离相关文档与脚本差异，确保术语统一为 move、button、wheel 三类独立操作。
2. 如需正式安装验收，以管理员刷新最新发布包并在 installed-only 环境再次验证 25/25。
3. 用户确认后有意提交并推送源码与文档；不提交发布目录和测试日志。

### 活跃上下文文件

- `README.md`
- `docs/CURRENT_PROGRESS.md`
- `scripts/Test-RemoteControllerUi.ps1`
- `artifacts/test-results/ui-rerun-separated-mouse-wheel-empty-clipboard-20260807-215055.log`

### 特殊指令

- 不得把定位动作隐式合并到按键或滚轮命令；验收必须保留“先单独定位、再单独输入”的操作边界和实际结果。
