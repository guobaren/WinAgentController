# 交接

- 更新时间：2026-08-12
- 工作目录：`E:\WinAgentController`
- 当前分支/基线：`main` @ `5dde471`（功能、测试与此前文档已提交并推送；本轮仅增量更新验收文档）

## 当前目标

已完成跨机器 1 GiB copy 在 256 MiB 检查点后的真实断线续传验收；当前没有处于执行状态的目标，隔离目标失败回滚为下一排队项。

## 已完成

+ 二进制 copy/update、实时速度、目标自动登记、CLI 缺参边界和 detached SYSTEM 更新链路已实现；不再兼容旧 Agent JSON 路径。
+ copy 双向和 update 单块断线重连、`wireBytes/retransmittedBytes`、默认 256 MiB 检查点、块幂等及最终 SHA-256 已通过真实 TLS/文件系统集成测试。
+ detached runner 已持久化 stdout/stderr，默认 3600 秒无 durable result 时从 `Applying` 收敛为 `Failed`。
+ ConPTY 取消测试竞态、同设备旧指纹档案替换和当前目标下 CLI 缺参越界均已修复。
+ SDK 8.0.422 已安装；Debug/Release Build 0 warnings/0 errors，完整测试均为 306/306。
+ 当前正式包为 19 个根文件、0 子目录、708,981,117 字节；本机/对端 Skill 上次同步为 5/5。
+ 最终标准升级会话 `13c0a35e-4e9f-414f-94e4-cefa938329a8` 无人工干预完成：`Succeeded/0`、708,981,117/708,981,117，纯上传 8.873 秒、76.20 MiB/s，`retransmittedBytes=0`，全流程 30.5 秒。
+ 最终对端矩阵通过：result 日志路径正确，stdout 790 字节、stderr 0 字节；更新任务 0，Agent/Broker Running，UI Ready/session 1/YUKIKAZE/1920×1080 主屏；设备/固定指纹/配对和四项安装哈希保持一致。
+ 首次会话暴露 runner 新日志参数与已安装 applier 的升级过渡缺陷；日志参数已改为可省略并从 ResultPath 推导，定向测试 2/2，通过 staging 单文件修复恢复首会话后，再由最终包标准升级确认修复。
+ 跨机器真实断线会话 `transfer-52ecf7e57e0f453c935daffbda93999d` 在 256 MiB 检查点后由 SYSTEM 看门狗强制终止 Agent PID，15 秒后恢复并以同一 session 自动续传：`bytes=1,073,741,824`、`wireBytes=1,132,462,080`、`retransmittedBytes=58,720,256`，19.161 秒，min/max/avg=10.02/111.97/56.37 MiB/s。
+ 断线续传源文件、远端文件和回下载文件 SHA-256 三方一致；回下载 1 GiB 为 9.421 秒、108.70 MiB/s、重传 0。远端 16 个 `codex-resume-*`、3 个 transfer staging、2 个触发工件和 1 个计划任务已清零，本机 2 个 `rc-copy-resume-*` 临时根已移入回收站；Running jobs=0，Agent/Broker 与 UI 恢复通过。
+ 历史性能、升级和 UI 验收证据已压缩到审计矩阵与历史里程碑，不再作为当前目标展开。

## Next Steps（优先级）

1. [P2 / 排队] 隔离目标注入 detached 非零退出、result 缺失和安装回滚。
2. [P3 / 排队] 建立 Windows CI、覆盖率和发布制品哈希/许可清单。
3. [P3 / 排队] 覆盖卸载数据保留、服务账户切换和连续多次断线压力回归。

## 活跃上下文文件

- `README.md`
- `docs/CURRENT_PROGRESS.md`
- `docs/交接.md`
- `docs/审计.md`
- `src/Rc.Agent/Updates/UpdateService.cs`
- `src/Rc.Agent/Control/TlsControlListener.cs`
- `src/Rc.Agent/Files/FileTransferService.cs`
- `src/Rc.Cli/Commands/UpdateCommand.cs`
- `src/Rc.Cli/Commands/FileCommand.cs`
- `src/Rc.Cli/Targets/ControllerTargetStore.cs`
- `src/Rc.Cli/Targets/TargetArgumentResolver.cs`
- `src/Rc.Cli/Security/AuthenticatedControlConnection.cs`
- `src/Rc.Contracts/UpdateContracts.cs`
- `src/Rc.Contracts/ControlChannelContracts.cs`
- `tests/Rc.Agent.Tests/Control/ControlSessionIntegrationTests.cs`
- `tests/Rc.Agent.Tests/Cli/CliCommandLiveTests.cs`
- `tests/Rc.Agent.Tests/Files/FileTransferServiceTests.cs`
- `tests/Rc.Agent.Tests/Updates/UpdateServiceTests.cs`
- `tests/Rc.Agent.Tests/InstallationScriptTests.cs`
- `tests/Rc.TaskHost.Tests/TaskHostRunnerTests.cs`
- `scripts/Invoke-RemoteControllerDetachedUpdate.ps1`
- `.agents/skills/operate-win-agent-controller/`

## 特殊指令

- 常规安装、刷新和升级必须保持 `RegenerateIdentity=false`；除非用户明确要求重置身份，否则不得解除配对、轮换证书或删除 DataRoot。
- UDP 发现只用于定位；只有成功完成显式固定指纹连接后，才允许用同设备 ID 的新指纹替换旧档案。
- 不在聊天、普通日志或源码中记录一次性配对码、控制端私钥、Agent 私钥或完整固定指纹。
- 更新成功必须同时核对 detached result、固定指纹 probe、无副作用认证命令、服务/UI 状态及相关哈希；上传完成或 bootstrap job 结束不能单独证明升级成功。
- 测试必须读取实际输出并逐项断言；构建成功、退出码 0、模拟测试或服务启动不能替代真实链路/UI 验收。
- UI 验收要求活动且解锁的普通用户桌面；鼠标 move/button/wheel 必须分开操作和验证，默认步骤间隔为 400 ms。
- `Rc.TestRemoteControl.exe` 只允许用于已授权可信测试网，不进入正式发布包，也不构成正式认证边界。
- `target refresh` 首次启用发现防火墙规则时需要管理员权限。
- 功能与测试已提交为 `5dde471` 并推送；本轮仅有验收文档增量，后续提交前仍须整体复核工作区。

## 历史里程碑

| 日期 | 里程碑 |
| --- | --- |
| 2026-07-16 | 完成 Hyper-V 双机核心链路、UI 25 项、浏览器 DOM、持久任务和文件续传验收。 |
| 2026-08-03 至 2026-08-07 | 修复初始化 StrictMode、身份保留默认值、目标档案、空剪贴板和输入操作分离，并完成本机 UI 25/25。 |
| 2026-08-11 | 完成 detached SYSTEM 更新生命周期、流式文件传输、SCP 对照、成功连接自动登记、实时速度、`binary-update-v1`、真实对端升级及健康矩阵。 |
| 2026-08-11 | 修复 ConPTY 测试启动竞态，Debug/Release 完整测试收口为 285/285。 |
| 2026-08-12 | 同设备旧指纹档案改为成功验证后自动替换；修复 CLI 缺参结构化错误；移除旧 Agent 回退；本机/对端 Skill 同步为 5/5，完整测试 297/297。 |
| 2026-08-12 | 完成 copy 双向/update 真实 TLS 断线重连、256 MiB 检查点、重传字节拆分、detached 日志/超时；安装 SDK 8.0.422，Debug/Release 306/306。 |
| 2026-08-12 | 修复 detached runner 升级过渡参数；708,981,117 字节最终包标准升级 `Succeeded/0`，速度、日志、身份、服务、UI、任务清理和安装哈希矩阵通过。 |
| 2026-08-12 | 完成 1 GiB 在 256 MiB 检查点后的目标侧强制断线与同 session 续传；精确核对逻辑/线上/重传字节、三方 SHA-256、健康恢复及全部测试残留清理。 |
