# 交接

- 更新时间：2026-08-14
- 工作目录：`E:\WinAgentController`
- 当前分支/基线：`main` @ `e1fbc32`（与远端一致）；工作区含未提交改动：目录索引、SDK 兼容策略、`TCP_NODELAY`、性能证据与本文档，待提交。

## 当前目标

大量小文件目录传输慢路径已修复并完成真实验收：根因是目录下载每分片的 JSON/1 字节就绪信号在未启用 `TCP_NODELAY` 时触发 Nagle 与 Windows 延迟 ACK（约 20 ms/文件），叠加 Agent/CLI 端 O(N²) 索引扫描。双端启用 `TCP_NODELAY` 并建立会话级索引后，13,667 文件真实目录下载由 323.634 秒降至 40.443 秒，达到同数据集 SCP 吞吐的 87.5%（进程口径）。剩余工作为提交本批改动。

## 已完成（本轮关键结论）

+ 目录传输慢路径修复：`FileTransferService` 会话级 manifest/receipt/完成路径索引 + CLI 恢复 `HashSet`；`PinnedTlsConnection`/`TlsControlListener` 双端 `NoDelay=true`，不改变协议、完整性或续传语义。
+ 性能证据（全部 SHA-256 一致、重传 0）：13,667 文件真实目录下载 40.443 秒/2.53 MiB/s（修复前 323.634 秒/0.32 MiB/s），WAC 进程/内部达 SCP 的 87.5%/92.0%；100 小文件 WAC 上传/下载 59.62/75.46 MiB/s（SCP 65.61/74.74）；1 GiB 96.53/106.97 MiB/s。
+ 验证：SDK 10.0.301 下 Debug/Release 全方案 309/309、Build 0 warnings/0 errors；新增 2,048 文件流式回归；`global.json` 保留 8.0.422 下限基线 + `rollForward=latestMajor`。
+ 发布与升级：19 根文件、689,693,449 字节完整包；标准升级 `3dabe2fc-51a6-4d22-bd2c-5e9028149f06` 为 `Succeeded`、纯上传 8.769 秒/75.00 MiB/s、重传 0；身份/配对/认证/服务/UI 注册均保持。
+ 断线续传：1 GiB 在 256 MiB 检查点后由 SYSTEM 看门狗真实终止 Agent，15 秒后同 session 自动续传完成，逻辑/线上/重传字节与三方 SHA-256 一致；所有远端/本机残留已清理。
+ 持久 SCP 密钥：`C:\Users\YUKIKAZE\.ssh\id_ed25519_winagentcontroller` + `winagentcontroller` alias + 已核验 `known_hosts`，公钥登记对端管理员授权文件；私钥不入库。
+ 更新残留治理：`UpdateService` 在 detached/非 detached 两条 `Succeeded` 收敛路径删除 `payload\`，保留 `update-state.json` 终态记录；`UpdateServiceTests` 定向 10/10、全量 173/173，发布包已重建。真实对端三次升级（`9e96f045...`、`c4412ac0...`、`eb3a26af...`）均 `Succeeded`，升级后 `payload` 自动删除、`updates` 仅 0.01 MB，Agent/Broker Running。
+ 进度可见性：copy/update 上传每秒输出 `bytes/total/percent/currentMiB/s`；`update apply --wait` 轮询每秒输出 `received=X/Y (P%)`，可区分上传/应用阶段失败；reporter 定向测试 4/4，真实对端 copy 双向与升级链路验证通过。
+ 自动发布：`.github/workflows/release.yml` 按 `v*` tag 触发，构建后发布含完整包 + 双版本 README + `LICENSE`（MIT）+ `VERSION.txt` + `SHA256SUMS.txt` 的 zip；`docs/安装说明.md` 为 zip 内精简 README 来源。

## Next Steps（优先级）

1. [P1 / 可选协议演进] 仅当性能目标明确高于当前相对 SCP 87.5% 时，再设计批量 manifest/打包流或多文件复用协议。
2. [P2 / 工程化] 补充 detached 目标侧失败/回滚注入；建立覆盖率与第三方许可清单（Windows CI 已配置于 `.github/workflows/windows-ci.yml`，自动发布由 `v*` tag 触发的 `.github/workflows/release.yml` 提供）。

## 活跃上下文文件

- `README.md`、`docs/`（交接 / 审计 / 安装说明）、`LICENSE`、`global.json`、`.github/workflows/`（windows-ci.yml / release.yml）
- `src/Rc.Agent/`：`Control/TlsControlListener.cs`、`Files/FileTransferService.cs`、`Updates/UpdateService.cs`
- `src/Rc.Cli/`：`Commands/UpdateCommand.cs`、`Commands/FileCommand.cs`、`Targets/ControllerTargetStore.cs`、`Targets/TargetArgumentResolver.cs`、`Security/AuthenticatedControlConnection.cs`、`Security/PinnedTlsConnection.cs`
- `src/Rc.Contracts/`：`UpdateContracts.cs`、`ControlChannelContracts.cs`
- `tests/`：`Rc.Agent.Tests`（ControlSessionIntegrationTests / CliCommandLiveTests / FileTransferServiceTests / UpdateServiceTests / InstallationScriptTests）、`Rc.TaskHost.Tests/TaskHostRunnerTests.cs`
- `scripts/Invoke-RemoteControllerDetachedUpdate.ps1`、`.agents/skills/operate-win-agent-controller/`

## 特殊指令

- 安装/刷新/升级保持 `RegenerateIdentity=false`；不得擅自解除配对、轮换证书或删除 DataRoot。
- UDP 发现仅用于定位；仅成功固定指纹连接后允许同设备新指纹替换旧档案。
- 不在聊天、日志或源码中记录配对码、控制端/Agent 私钥或完整固定指纹。
- 更新成功必须核对 detached result、指纹 probe、认证命令、服务/UI 状态与哈希；上传完成不能单独证明升级成功。
- UI 验收要求活动且解锁的普通用户桌面；鼠标 move/button/wheel 分开验证，默认间隔 400 ms。
- `Rc.TestRemoteControl.exe` 仅限已授权可信测试网，禁止进入正式发布包。
- `target refresh` 首次启用发现防火墙规则需要管理员权限。
- 提交/推送前整体复核工作区并确认远端分支无新增提交（当前 HEAD=`e1fbc32` 与 `origin/main` 一致）。
- SDK 策略：8.0.422 为原始验证基线，默认允许更高 SDK，出现明确兼容问题再按证据固定版本。
- SCP 访问使用 `winagentcontroller` alias 与专用私钥；不复制、打印或提交私钥。
- 当前 Web 进程为验收启动的后台进程；未配置服务/计划任务/登录自启动。
- 测试脚本默认清理本地 `artifacts` 测试数据与远端基准目录，避免残留；需保留现场排查时用 `-KeepArtifacts` 重跑。`artifacts/` 除 `publish/`、`test-tools/` 外均为一次性产物，可随时删除。

## 文档约定

- 事实来源优先级：当前实现与 CLI 用法 > 部署脚本 > `README.md` > 历史记录（`docs/交接.md`、`docs/审计.md`）。
- `README.md`：用户入口，维护功能、部署、命令和安全边界（详细版；GitHub Release zip 内以 `README-详细版.md` 附带）。
- `docs/安装说明.md`：精简安装指南（给人看；GitHub Release zip 内以 `README.md` 附带）。
- `docs/交接.md`：当前目标、最近完成项和前三项执行路线。
- `docs/审计.md`：质量矩阵、已知风险、技术债和信心边界。
- `tools/Rc.TestRemoteControl`：可信测试局域网中的临时独立命令通道，不属于产品或发布包。
- `artifacts/`：仅 `publish/`（脚本默认 CLI 路径）与 `test-tools/`（构建输出）需要保留；其余一次性验收/基准目录不入库，测试脚本成功路径默认清理（`-KeepArtifacts` 可保留现场），历史残留可随时删除。
- 历史失败只有仍能解释当前风险时才保留；已被取代的过程日志压缩为里程碑。

## 历史里程碑

| 日期 | 里程碑 |
| --- | --- |
| 2026-07-16 | Hyper-V 双机核心链路、UI 25 项、浏览器 DOM、持久任务与文件续传验收。 |
| 2026-08-03 ~ 08-07 | 修复初始化 StrictMode、身份保留、目标档案、空剪贴板与输入分离；本机 UI 25/25。 |
| 2026-08-11 | detached SYSTEM 更新、流式传输、SCP 对照、自动登记、实时速度、`binary-update-v1` 真实对端升级；测试收口 285/285。 |
| 2026-08-12 | 旧指纹自动替换、CLI 缺参修复、copy/update 断线重连与 256 MiB 检查点、detached 日志/超时；SDK 8.0.422 基线，测试 306/306；1 GiB 检查点后真实断线同 session 续传。 |
| 2026-08-14 | 定位并修复 O(N²) 索引 + 双端 `TCP_NODELAY`，13,667 文件真实目录下载达 SCP 87.5%；WAC/SCP 双向基准；最终 689,693,449 字节包标准升级全部通过。 |
