# 审计

- 审计日期：2026-08-03
- 工作目录：`D:\RemoteController`
- 审计范围：一键 Agent 初始化失败诊断、脚本修复、发布包与本地构建/测试验证。

## 结论

- 结果：**代码修复完成；真实目标机初始化尚未重跑，不能宣称端到端通过。**
- 根因：`scripts/Setup-RemoteControllerAgent.ps1:232` 读取了 `Rc.Agent.exe identity` 不提供的 `paired` 属性；`Set-StrictMode -Version Latest` 将其升级为 `PropertyNotFoundStrict`，导致初始化实际完成后仍返回退出码 1。
- 修复：`scripts/Setup-RemoteControllerAgent.ps1:232-237` 使用 `PSObject.Properties['paired']` 安全读取，缺失字段时输出“not reported”。
- 信心指数：**0.95**（根因通过脚本实现、Agent identity 序列化代码、Windows PowerShell StrictMode 复现和修复后模拟输出共同确认；尚缺真实目标机重跑）。

## 验证矩阵

| 类别 | 结果 | 证据 |
| --- | --- | --- |
| 根因复现 | Pass | Windows PowerShell `StrictMode` 下访问缺失 `paired` 属性实际返回退出码 1，并报告 `PropertyNotFoundStrict`。 |
| 修复后脚本片段 | Pass | 缺失属性安全检查输出 `Currently paired: not reported by the local identity command.`，退出码 0。 |
| PowerShell AST | Pass | `scripts/Setup-RemoteControllerAgent.ps1` 语法错误数为 0。 |
| 发布包 | Pass | `scripts/Setup-RemoteControllerAgent.ps1` 与 `artifacts/publish/Setup-RemoteControllerAgent.ps1` SHA-256 一致；发布脚本完成。 |
| Release Build | Pass | `dotnet build Rc.RemoteController.sln --configuration Release --no-restore`：0 warning、0 error。首次 no-restore 因旧回退包目录缓存失败，执行 `dotnet restore --force-evaluate` 后重跑通过。 |
| 单元测试 | Pass | 四个测试程序集合计 252/252 通过：Contracts 118、TaskHost 15、PrivilegedBroker 3、Agent 116。 |
| Lint | 未配置 | 项目没有独立 lint 命令；Lint 错误数：未配置。Build 未报告编译警告。 |
| 测试覆盖率 | 未配置 | 未发现覆盖率配置或本次覆盖率报告。 |
| 真实目标机初始化 | 未执行 | 尚未复制新包并重跑会停止服务/可能轮换 TLS 身份的真实操作；不以本地构建、模拟片段或服务存在代替端到端结果。 |

## Known Issues / 风险

- `docs/CURRENT_PROGRESS.md:6-10` 保留历史 TaskHost/ConPTY 时序失败记录；本次 Release 测试当前为 252/252，但历史验收记录未删除。
- `scripts/Setup-RemoteControllerAgent.ps1:181-187` 与 `scripts/RemoteController.Agent.config.json:8` 的默认 `RegenerateIdentity=true` 会解除旧配对并请求新 TLS 身份；对已有目标刷新必须显式改为 `false`，否则需要重新核对指纹并配对。
- `scripts/Setup-RemoteControllerAgent.ps1:197-204` 只等待服务达到 Running，再通过本地 identity 文件确认 Agent 初始化；真实目标机仍需验证服务启动、数据根 ACL、身份文件和控制端 probe 的完整链路。
- `C:\Program Files\RemoteController` 当前安装副本未由本次代码修复自动替换；必须使用新生成的完整 `artifacts\publish` 包更新目标后再验证。

## 技术债

- 为一键初始化增加无副作用的回归测试：构造 identity JSON 缺失可选字段并执行最终汇总输出，确保 StrictMode 下不会再次回归。
- 将 Agent identity 的 JSON 契约集中到共享 DTO/测试，避免脚本自行假设服务端返回未声明字段。
- 为脚本增加持久化日志或结构化诊断选项，保留失败阶段和完整 PowerShell 异常；当前 `.cmd` 只显示通用退出码尾部。

## 自我评价

- 诊断证据充分，修复范围最小，没有触碰服务身份、配对数据或用户无关代码。
- 本次没有弱化验收条件：单元/构建结果与真实目标机验证明确分开。

## 工作树收尾检查

- `git diff --check`：Pass，退出码 0。
- 工作树仍含用户原有及本次相关未提交改动；本次未执行重置、清理、暂存或提交。

## 提交前测试复核

- `dotnet test Rc.RemoteController.sln --configuration Release --no-build --verbosity minimal`：Pass，Contracts 118、TaskHost 15、PrivilegedBroker 3、Agent 116，共 252/252。
- 本次提交范围包含当前已存在的 CLI 目标档案、卸载入口、Agent 初始化修复、测试和双文档；真实目标机初始化仍未执行。

## 最新代码同步检查

- `git pull --ff-only origin main`：Pass，快进到 `91223e5`。
- 同步提交：`fix: reject duplicate target fingerprints`；新增重复 `--fingerprint` 拒绝逻辑及测试。
- 本次操作未运行测试；此前提交已有 252/252 测试记录，不能将其自动扩展为本次远端新增提交的验证结果。

## 配对码输出诊断

- 源码结论：`Setup-RemoteControllerAgent.ps1:127-129` 读取 `ArmPairing`；为 `true` 时 `:211-218` 调用 `arm-pairing` 并打印 `oneTimeCode`，为 `false` 时 `:219-220` 明确跳过。
- Agent 契约结论：`src/Rc.Agent/Security/LocalAdminCommand.cs:66-118` 的 `arm-pairing` 成功响应包含 `OneTimeCode`；当前源码不存在“成功但正常路径不打印配对码”的分支。
- 诊断边界：尚未取得目标机实际配置、完整控制台输出和运行脚本哈希，因此旧发布包、外部配置 `ArmPairing=false` 或查看父窗口仍是待确认原因。
- 验证状态：未运行真实目标机测试；不得将本地源码检查视为目标机配对成功。

## 2026-08-07 提交前质量检查

- 修改范围：4 个已跟踪文件；无未跟踪文件。
- PowerShell AST：Pass，0 个语法错误。
- Build：未重新执行完整 Build；本次 `dotnet test --no-restore` 编译/运行通过。
- 单元测试：Pass，253/253（Contracts 118、TaskHost 15、PrivilegedBroker 3、Agent 117）。
- Lint：未配置，错误数未配置。
- 测试覆盖率：未配置。
- `git diff --check`：Pass；存在 LF/CRLF 转换提示，不属于内容错误。
- 真实目标机验证：未执行，不能据此确认安装、服务、配对或远程控制链路通过。

## Known Issues / 风险

- `scripts/Setup-RemoteControllerAgent.ps1` 的提升流程仍依赖 Windows UAC 与目标机权限；本地 AST/单元测试未覆盖真实管理员提升和安装副作用。
- `scripts/Setup-RemoteControllerAgent.cmd` 的新窗口行为与退出码传播未在真实目标机验证。
- `artifacts/publish` 未纳入本次提交；目标机仍需使用与源码匹配的完整发布包。

## 技术债

- 增加不触发真实安装副作用的 `.cmd` 退出码传播回归测试。
- 增加提升后日志转发、临时日志清理和特殊路径引用的脚本集成测试。

## 自我评价

- 提交范围与用户当前工作树一致，未执行重置、清理或覆盖。
- 代码检查和单元测试结果明确，但真实目标机验证仍为空缺。
- 信心指数：0.88（本地质量检查）；端到端部署信心不作推断。

## 2026-08-07 目标机安装失败质量记录

- 用户实际安装结果：Failed；在 `[1/5] Installing or refreshing the service package...` 后返回 exit code 1。
- 根因证据：不充分；用户提供的片段缺失 PowerShell 异常正文，当前不得归因到具体命令。
- 模拟检查：Pass；`Install-RemoteController.ps1 -WhatIf` 退出码 0，但未执行副作用。
- 单元测试/Build：本次诊断未重新运行；此前本地记录为 253/253 单元测试通过。
- 真实目标机安装：Failed；尚未取得完整异常、目标机服务配置和安装阶段细分结果。
- Lint：未配置。测试覆盖率：未配置。

## Known Issues / 风险

- `scripts/Setup-RemoteControllerAgent.ps1:194-196` 对安装子脚本只报告通用失败路径；缺失完整异常时无法确定是复制、服务控制、权限还是计划任务阶段。
- `scripts/Setup-RemoteControllerAgent.ps1:40-67` 的提升子进程日志转发需要结合目标机完整输出验证，当前不能确认原始异常是否被完整保留。
- `artifacts/publish` 与 `scripts` 安装脚本哈希一致，但包一致不等于目标机安装成功。

## 自我评价

- 本次未修改生产脚本或目标机状态，仅进行了源代码、包哈希、服务状态和 `-WhatIf` 检查。
- 当前诊断信心：0.72（可定位到 `[1/5]` 子脚本阶段）；具体根因信心：0.20（缺少异常正文）。

## 2026-08-07 日志采集复核

- 用户执行了日志采集命令，但返回片段仅显示日志路径，没有安装脚本输出或异常。
- Codex 会话中检查该 Temp 路径：文件不存在；日志证据不可用。
- 真实目标机安装状态：仍为 Failed/未确认；未因缺少日志而修改脚本或重试副作用操作。

## Known Issues / 风险

- 自提升脚本可能在新的管理员进程中执行，原 PowerShell 管道不一定能直接取得提升进程的完整输出；需要在实际执行会话确认日志文件是否生成。

## 2026-08-07 根因确认与修复验证

- 真实错误：Failed，`D:\WinAgentController\artifacts\publish\Setup-RemoteControllerAgent.ps1:176` 抛出 `Missing package artifact: Rc.Agent.exe`。
- 根因：复制发布包入口的 `$PSScriptRoot` 与仓库 `scripts` 入口不同，原回退逻辑无法稳定解析包目录；配置的 `SourcePath="."` 又相对 `scripts` 配置目录解析。
- 修复范围：`scripts/Setup-RemoteControllerAgent.ps1:165-187`；回退候选优先当前脚本目录，其次仓库 `artifacts\\publish`，并从选中目录加载安装脚本。
- 回归检查：Pass；候选解析选择 `D:\WinAgentController\artifacts\publish`。
- PowerShell AST：Pass，0 个语法错误。
- 单元测试：Pass，253/253（Contracts 118、TaskHost 15、PrivilegedBroker 3、Agent 117）。
- 发布包：本地 `artifacts/publish/Setup-RemoteControllerAgent.ps1` 已同步；完整发布包尚未重新生成/复制到目标机。
- 真实目标机复测：未执行；当前修复仍未获得端到端通过证据。

## Known Issues / 风险

- 目标机必须获得更新后的 Setup 脚本；仅重跑旧发布包不会包含本次路径解析修复。
- `RegenerateIdentity=true` 默认行为会清除旧配对并轮换身份；已有配对目标刷新前必须审慎设置。

## 技术债

- 为发布包入口与仓库脚本入口分别增加不触发安装副作用的自动化回归测试。

## 自我评价

- 根因已由目标机完整异常与源代码路径推导相互印证；修复范围局部且未触碰安装服务逻辑。
- 当前代码修复信心：0.90；真实目标机端到端信心：未验证。

## 2026-08-07 本机 localhost/UI acceptance 质量记录

- 安装服务状态：Pass（仅检查）；`RemoteControllerAgent`、`RemoteControllerBroker` 均 `Running`，生产监听端口为 `43001`。
- 生产 Agent TLS probe：Pass；`127.0.0.1:43001` 可完成指纹固定的 probe，并报告已配对。
- 生产 Agent UI acceptance：Failed；直接运行脚本返回 `The session controller identity does not match the paired controller.`，原因是当前 CLI 控制端身份与已安装 Agent 的既有配对控制端不一致。
- 本机模拟 Agent/UI 链路：Pass；在不改动生产服务身份的前提下，使用临时用户数据根和临时端口 `43002` 启动 Agent/UI Agent，并通过当前用户 CLI 配对。
- 真实可见 UI 验收：Pass；使用 `D:\WinAgentController\artifacts\publish\Rc.UiTestApp.exe` 的可见窗口，`Test-RemoteControllerUi.ps1` 实际输出 25 个 `[PASS]`，最终输出 `UI acceptance passed (25 checks)`，退出码 0；剪贴板读回值与期望值一致。
- 单元测试：Pass，253/253（Contracts 118、TaskHost 15、PrivilegedBroker 3、Agent 117；此前已完成）。
- Build：Pass（此前 Release 构建记录）；Lint：未配置；测试覆盖率：未配置。
- 临时资源清理：Pass；临时 Agent/UI Agent 已停止，临时数据根已删除；生产服务未停止，`Rc.UiTestApp.exe` 窗口仍保持运行。

## Known Issues / 风险

- `D:\WinAgentController\scripts\Test-RemoteControllerUi.ps1:241-243` 在原始剪贴板为空时仍会把空字符串传给带有 `[ValidateNotNullOrEmpty()]` 的 `UiArguments` 参数，导致验收动作全部完成后在 finally 恢复阶段失败。
- `D:\WinAgentController\scripts\Test-RemoteControllerUi.ps1` 的一次运行成功只能证明当前用户 CLI、临时 Agent、临时 UI Agent 和可见 UI 测试窗口组成的本机模拟链路；不能替代已安装 `43001` 服务的当前配对控制端验证。
- 未执行 unpair、身份重生成或生产服务重配对；`43001` 的 UI 身份不匹配问题仍待用户明确授权后处理。

## 技术债

- 增加空剪贴板恢复测试，并将测试夹具清理失败计为明确失败。
- 增加“已配对控制端身份不匹配”的诊断提示或本地状态检查，减少 UI acceptance 首轮重试等待。

## 自我评价

- 质量结论按模拟 Agent、生产 localhost probe、生产 UI acceptance、单元测试和构建分别记录，没有用临时模拟结果替代生产服务 UI 通过结论。
- 当前本机模拟 UI 链路信心：0.98；已安装生产服务 UI 链路信心：未通过，阻塞点为既有配对控制端身份不匹配。

## 2026-08-07 本机重编译、一键重装与生产服务 UI 验收

- 测试覆盖率：未配置。
- Lint 错误数：未配置独立 Lint 工具，未声称为 0。
- Build：Pass。scripts\Publish-RemoteController.ps1 -Configuration Release 成功生成 Windows x64 自包含完整包；所需 7 个可执行文件和部署脚本均存在，发布包 Setup 脚本与源码 SHA-256 一致。
- 单元测试：Pass。dotnet test Rc.RemoteController.sln --configuration Release --no-restore --verbosity minimal，253/253 通过（Contracts 118、TaskHost 15、PrivilegedBroker 3、Agent 117）。
- 一键卸载：Pass。执行 artifacts\publish\Uninstall-RemoteController.cmd 后退出码为 0；实际检查确认两项服务、UI 计划任务、C:\Program Files\RemoteController、C:\ProgramData\RemoteController、43001 监听及产品进程均已消失。
- 一键安装：Pass。执行 artifacts\publish\Setup-RemoteControllerAgent.cmd 后退出码为 0；实际检查确认 RemoteControllerAgent 和 RemoteControllerBroker 均为 Running、启动类型为 Automatic、43001 正在监听、RemoteControllerUiAgent 计划任务存在且 Rc.UiAgent 进程运行。
- 安装内容一致性：Pass。Agent、PrivilegedBroker、TaskHost、UiAgent、UiTestApp、InteractiveTestApp 和 Rc.Cli 共 7 个已安装二进制均与最新 artifacts\publish 对应文件 SHA-256 一致。
- 本机生产服务配对与 probe：Pass。重装造成的旧配对清除后，当前 CLI 与 127.0.0.1:43001 完成新的控制端配对；指纹固定 TLS probe 成功，响应确认当前控制端已配对。敏感指纹和一次性配对码未写入本文件。
- 真实可见 UI 端到端：Pass。启动新发布包的 Rc.UiTestApp.exe 可见窗口后，对已安装生产服务 127.0.0.1:43001 运行 scripts\Test-RemoteControllerUi.ps1；25 个实际可见状态逐项输出 [PASS]，最终为 UI acceptance passed (25 checks)，退出码 0，且测试后的剪贴板字节内容与测试前一致。
- 验收边界：上述 UI 结果是“本机已安装生产 Agent + 当前用户 UiAgent + 当前 CLI + 可见 UI 窗口”的真实 loopback 链路，不是跨主机 LAN 结论。

## Known Issues / 风险（当前）

- D:\WinAgentController\scripts\Test-RemoteControllerUi.ps1:241-243：当测试前剪贴板文本为空，finally 中将空字符串传入 UiArguments 会触发参数绑定失败。为确保本次生产 UI 的 25 项动作和脚本退出码可验证，外层夹具仅在检测到空剪贴板时预填非空标记，测试结束后再恢复原始空文本；该脚本清理分支本身尚未修复，不能视为已覆盖。
- D:\WinAgentController\scripts\RemoteController.Agent.config.json:8 的 RegenerateIdentity=true 会删除已有配对；本次为用户明确要求的卸载重装，副作用已发生，后续重装应先确认是否需要保留既有控制端。
- 安装自提升输出含一次性配对码；运行日志需避免进入共享目录、提交内容或普通聊天记录。
- 当前未做异机 LAN、不同桌面会话或断线恢复的端到端验证。

## 技术债（当前）

- 为空剪贴板恢复路径增加自动化回归，并修复 Test-RemoteControllerUi.ps1 的 finally 参数传递，使其将恢复失败判为明确失败。
- 为发布包入口 SourcePath="." 和仓库脚本入口分别保留无副作用的路径解析测试。
- 为跨机器/跨会话场景建立可重复的 probe、配对、UI 输入与接收端实际结果采集记录。

## 自我评价（当前）

- 代码构建、单元测试、发布包完整性、一键卸载、一键安装、二进制一致性、当前控制端配对/probe 和已安装服务可见 UI 25/25 验收均有独立实际证据，没有将其中任一项替代为另一项。
- 同机已安装生产服务端到端信心：0.98；跨机器 LAN 与空剪贴板 finally 分支信心：未验证/待修复。

## 2026-08-07 空剪贴板修复后 UI 重跑审计

- 测试覆盖率：未配置。
- Lint 错误数：未配置独立 Lint 工具。
- Build：Pass（此前 Release 发布包重建成功）。
- 单元测试：Pass，253/253（Contracts 118、TaskHost 15、PrivilegedBroker 3、Agent 117）。
- 真实可见 UI 验收：Failed。本轮在 `127.0.0.1:43001` 和可见 `Rc.UiTestApp.exe` 上实际通过 11 项；左键按下断言期望 `Verification: mouse:down:left`，实际为上一树节点状态 `Verification: tree:top:collapsed`。日志：`D:\WinAgentController\artifacts\test-results\ui-rerun-empty-clipboard-20260807-212046.log`。
- 剪贴板 finally 验证：Failed/未取得。测试失败后再次经实际 UI 路径读取剪贴板时，Agent 返回 `The active UiAgent cannot be reached`，不能将本轮视为初始空剪贴板恢复已通过。
- 运行环境风险：同时存在一个 `C:\Program Files\RemoteController\Rc.UiAgent.exe` 和多个手动启动的 `D:\WinAgentController\artifacts\publish\Rc.UiAgent.exe`。手动实例若未赋予 LocalService 控制管道权限，会导致 Agent 指向不可达的活跃 UiAgent；当前多实例竞争使结果不稳定。

## Known Issues / 风险（增量）

- `D:\WinAgentController\scripts\Test-RemoteControllerUi.ps1:199-208`：鼠标左键按下操作后可见验证文本未更新，当前真实运行失败；需在单 UiAgent 环境复现并定位输入注入或 UI 事件处理边界。
- `D:\WinAgentController\scripts\Test-RemoteControllerUi.ps1:242-250` 的空剪贴板恢复分支已在源码中改为 `clipboard clear`，但尚未在稳定单 UiAgent 的完整真实 UI 链路上取得 25/25 和清理读回证据。
- 本轮期间多个手动 `artifacts\publish\Rc.UiAgent.exe` 仍在运行；在清理或正式刷新前，不得将 localhost UI 结果解释为最新发布包正式安装验收。

## 技术债（增量）

- 为手动 UiAgent 调试启动提供显式的服务 SID 权限参数或专用测试入口，防止注册成功但 Agent 控制管道不可达。
- 为鼠标 down/up 事件加入可重复的端到端实际可见状态采集，保留操作请求、Agent 响应和 UI 验证文本的时间顺序。

## 自我评价（增量）

- 本轮严格以实际可见状态比对判定 Failed；未将前 11 项、脚本启动、单元测试或发布构建替代完整 UI 通过结论。
- 初始空剪贴板源码修复信心：0.85；稳定单 UiAgent 的真实 UI 验收信心：未通过。

## 2026-08-07 单 UiAgent 初始空剪贴板重跑审计（21:37）

- 测试覆盖率：未配置。
- Lint 错误数：未配置独立 Lint 工具。
- Build：Pass（沿用本次最新 Release 发布包重建结果）。
- 单元测试：Pass，253/253（沿用本次修复后的执行结果）。
- 测试环境隔离：Pass。测试期间仅有一个最新发布目录 UiAgent，且 LocalService Agent 可连接其控制管道；此前多实例竞争已排除。
- 真实可见 UI 验收：Failed。实际通过 20 项，滚轮请求被拒绝，错误为 `Move the mouse within the explicit target before sending a mouse button or wheel event.`。日志：`D:\WinAgentController\artifacts\test-results\ui-rerun-single-agent-empty-clipboard-20260807-213741.log`。
- 初始空剪贴板 finally 恢复：Pass。脚本异常退出后，实际 UI 协议立即读回 0 字节，1 秒后再次读回 0 字节。
- 环境恢复：Pass。测试后仅保留一个安装目录 `C:\Program Files\RemoteController\Rc.UiAgent.exe`，发布目录隔离实例已停止。

## Known Issues / 风险（增量）

- `D:\WinAgentController\scripts\Test-RemoteControllerUi.ps1:213-218`：完成右键 up 后直接发送 wheel，没有在 wheel 前重新执行显式 window target 的 mouse move；当前输入控制器拒绝该请求，使后续键盘和剪贴板 UI 检查无法执行。
- 初始空剪贴板恢复已有真实失败路径证据，但完整 25/25 尚未取得；两者必须分别报告。

## 技术债（增量）

- 为每个要求显式目标上下文的鼠标 button/wheel 序列封装“先定位、再输入”的测试辅助函数，避免测试脚本依赖上一个操作遗留的目标状态。

## 自我评价（增量）

- 已排除多 UiAgent 干扰并取得可重复的明确失败边界；空剪贴板修复真实链路信心提升至 0.98。
- 完整同机 UI 验收仍为 Failed，未以 20 项通过或 finally 恢复成功替代 25/25 结论。

## 2026-08-07 滚轮定位修复后质量审计（21:45）

- 测试覆盖率：未配置。
- Lint 错误数：未配置独立 Lint 工具。
- PowerShell AST：Pass，`scripts/Test-RemoteControllerUi.ps1` 0 errors。
- Build/Publish：Pass，Release Windows x64 自包含发布包重新生成；测试脚本源码/发布副本 SHA-256 一致。
- 单元测试：Pass，253/253（Contracts 118、TaskHost 15、PrivilegedBroker 3、Agent 117）。
- 真实可见 UI 验收：Pass，25/25；包含左/中/右键、两次滚轮、键盘和剪贴板界面/协议读回的实际状态比对，退出码 0。
- 初始空剪贴板 finally 恢复：Pass；立即读回 0 字节，1 秒后仍为 0 字节。
- `git diff --check`：Pass，仅有工作区既有 LF/CRLF 转换提示。
- 日志：`D:\WinAgentController\artifacts\test-results\ui-rerun-wheel-fix-empty-clipboard-20260807-214502.log`。

## Known Issues / 风险（增量）

- `D:\WinAgentController\scripts\Test-RemoteControllerUi.ps1:210-220` 的滚轮前置定位缺陷已修复并通过真实 UI 验收；历史失败记录保留用于追踪。
- 最新发布目录 UiAgent 已通过隔离真实链路验证，但测试后恢复的是安装目录 UiAgent；尚未以管理员一键刷新确认安装目录已包含本轮最新二进制。
- 当前结论仍为本机 loopback，不覆盖异机 LAN、锁屏/UAC 安全桌面或不同登录会话。

## 技术债（增量）

- 为鼠标 button/wheel 测试动作抽取统一的显式定位辅助函数，减少重复代码并固定前置条件。

## 自我评价（增量）

- 修复点已由先前确定性失败转为同条件 25/25，且空剪贴板恢复有立即和延迟两次真实读回证据。
- 最新发布目录 localhost UI 链路信心：0.99；正式安装目录最新版本与跨机器链路仍需独立验证。

## 2026-08-07 输入操作分离目标审计（21:50）

- 基本目标文档：Pass。`README.md` 与 `docs/CURRENT_PROGRESS.md` 均明确鼠标 move、button、wheel 为独立操作，调用方和测试先单独定位，再执行按键或滚轮。
- PowerShell AST：Pass，`scripts/Test-RemoteControllerUi.ps1` 0 errors。
- Build/Publish：Pass。首次发布因仍运行的 `Rc.UiTestApp.exe` 锁定发布文件而 Failed；停止该测试进程后重新发布成功，源码/发布测试脚本 SHA-256 一致。
- 单元测试：本次仅调整 PowerShell 测试动作编排，未再次运行；上一轮同代码主体 Release 单元测试为 Pass，253/253。
- 真实可见 UI 验收：Pass，25/25，退出码 0。滚轮测试只在序列开始前单独 move 一次，随后 wheel up/down 均独立通过。
- 初始空剪贴板恢复：Pass；finally 后立即 0 字节，1 秒后仍为 0 字节。
- 环境恢复：Pass；测试后仅保留单个安装目录 UiAgent。
- 日志：`D:\WinAgentController\artifacts\test-results\ui-rerun-separated-mouse-wheel-empty-clipboard-20260807-215055.log`。

## Known Issues / 风险（增量）

- 最新脚本和发布目录 UiAgent 已完成 localhost 隔离链路验证，但正式安装目录仍未由管理员刷新到本轮最新发布包。
- 输入操作分离是 API/测试基本约束；后续重构不得让 button 或 wheel 隐式调用 move，也不得用组合测试掩盖单项失败。

## 自我评价（增量）

- 已按用户澄清纠正“每次 wheel 前重复定位”的测试编排，改为一次独立定位后分别验证两个滚轮操作，并获得 25/25 实际证据。
- 输入操作分离目标与 localhost 验收信心：0.99；正式安装目录和跨机器链路仍需独立验证。
